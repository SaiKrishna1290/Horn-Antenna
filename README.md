# Horn-Antenna
The program begins by creating a horn antenna object using the horn function. The physical dimensions of the horn are then manually specified using FlareWidth, FlareHeight, and Length. The antenna geometry is displayed in a 3D figure using the show() function.
